# EWM-Kapitsa-conductivity-calculation
Elastic-wave model of Kapitza conductance for multilayer interfaces (Zoeppritz + roughness averaging + Debye–Landauer), with AMM fallback. Produces two grouped figures and a single Excel workbook (one sheet per σ). Pure NumPy/Pandas/Matplotlib; modular Python package.
